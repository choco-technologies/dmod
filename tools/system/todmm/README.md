# todmm - DMM Manifest Generator

## Overview

The `todmm` tool scans a folder of DMF (DMOD Module Files) and generates a
`manifest.dmm` file listing every module found in the folder. Each entry in the
manifest is prefixed with a configurable base URL, making it easy to publish a
directory of modules to a remote registry.

It also supports a **zip mode** (`--zip`) where it scans for `.zip` release
packages (as generated in `build/packages/` when building in `DMOD_MODULE`
mode) and generates a manifest entry for each package.

## Usage

```bash
todmm <folder> <base_url> [-o output.dmm] [--zip]
```

### Arguments

- `<folder>` - Path to the folder containing `.dmf` / `.dmfc` module files (or `.zip` packages in zip mode) (required)
- `<base_url>` - Base URL or path prepended to each module entry (required)

### Options

- `-h, --help` - Print help message
- `-v, --version` - Print version information
- `-o <file>` - Output manifest file path (optional, defaults to `manifest.dmm`)
- `-z, --zip` - Scan for `.zip` release packages instead of `.dmf` / `.dmfc` files

## Examples

```bash
# Generate manifest.dmm from all modules in ./dmf
todmm ./dmf https://registry.example.com/modules

# Specify a custom output file
todmm ./dmf https://registry.example.com/modules -o output.dmm

# Base URL ending with '/' - no extra separator is added
todmm ./dmf https://registry.example.com/modules/ -o manifest.dmm

# Generate manifest from zip release packages
todmm ./build/packages https://releases.example.com/packages --zip
```

## Generated File Format

The tool generates a `.dmm` manifest file with the following format:

```dmm
# DMOD Manifest
# Generated by todmm from folder: ./dmf
# Base URL: https://registry.example.com/modules

kernel@1.0 https://registry.example.com/modules/kernel.dmf
driver@2.3.1 https://registry.example.com/modules/driver.dmf
app https://registry.example.com/modules/app.dmf
```

Each entry has the form `module_name@version url`. If the module has no version
in its header, the `@version` part is omitted.

In **zip mode**, the module name is derived from the filename (without the
`.zip` extension) and no version is included:

```dmm
# DMOD Manifest
# Generated by todmm from folder: ./build/packages
# Base URL: https://releases.example.com/packages

kernel https://releases.example.com/packages/kernel.zip
driver https://releases.example.com/packages/driver.zip
```

## How It Works

1. Opens the specified folder using `Dmod_OpenDir()`
2. Iterates over directory entries, filtering for `.dmf` and `.dmfc` files (or `.zip` files in zip mode)
3. For each module file, reads its header using `Dmod_ReadModuleHeader()` to
   obtain the module name and version. In zip mode, the module name is derived
   from the filename instead.
4. Constructs the download URL by joining `base_url` and the filename
5. Writes a manifest entry to the output `.dmm` file

## See Also

- [dmf-get](../dmf-get/README.md) - Package manager that consumes `.dmm` manifests
- [todmfc](../todmfc/README.md) - Tool for compressing DMF files to DMFC
- [todmp](../todmp/README.md) - Tool for creating DMP packages
- [DMM File Format](../../../tools/lib/dmod_manifest/vscode-dmm/example.dmm) - Example manifest
