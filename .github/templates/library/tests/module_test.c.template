#define DMOD_ENABLE_REGISTRATION ON
#include "dmod_test.h"
#include "@MODULE_NAME@.h"

static @MODULE_NAME@_t g_handle = NULL;

void dmod_test_setup(void)
{
    g_handle = @MODULE_NAME@_create();
}

void dmod_test_teardown(void)
{
    @MODULE_NAME@_destroy(g_handle);
    g_handle = NULL;
}

DMOD_TEST_STEP(@MODULE_NAME@_create)
{
    DMOD_TEST_EXPECT_NOT_NULL(g_handle);
}

DMOD_TEST_STEP(@MODULE_NAME@_is_valid)
{
    DMOD_TEST_EXPECT_TRUE(@MODULE_NAME@_is_valid(g_handle));
}

DMOD_TEST_STEP(@MODULE_NAME@_destroy_null)
{
    /* Destroying NULL must not crash. */
    @MODULE_NAME@_destroy(NULL);
}
